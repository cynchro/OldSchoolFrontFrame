server {
  listen 9000;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  # Root URL: send users to the demo app (repo has no single-page app at /).
  location = / {
    return 302 /example/;
  }

  location / {
    try_files $uri $uri/ =404;
  }
}
